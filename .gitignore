# API keys — never commit this
.env

# Google service account credentials — contains private key, never commit
google_credentials.json

# Python cache files
__pycache__/
*.pyc

# Mac system files
.DS_Store
